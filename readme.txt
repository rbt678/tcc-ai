Instalar todas as dependencias:

    pip install openai python-docx spacy chromadb sentence-transformers Flask

Após instalar o spacy, instale o modelo separadamente:

    python -m spacy download en_core_web_sm
